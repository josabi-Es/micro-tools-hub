# Postgres connection
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=orm_playground
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Key needed by every endpoint except health and ready
API_KEY=
